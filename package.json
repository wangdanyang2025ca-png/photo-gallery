{
  "name": "photo-gallery",
  "version": "1.0.0",
  "description": "优雅的照片展示画廊 + PWA 手机 App",
  "private": true,
  "scripts": {
    "postinstall": "node postinstall.js",
    "build": "mkdir -p public && cp index.html style.css app.js manifest.json sw.js server.js public/ && cp -r icons public/",
    "start": "node server.js"
  },
  "dependencies": {}
}
